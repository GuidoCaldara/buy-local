<%= simple_form_for @store, url: delivery_policy_store_path, method: :patch, remote: :true do |f| %>
  <%= f.input :delivery_fee, label: "Spese di consegna Standard in €" %>
  <%= f.input :free_delivery_threshold, label: "Importo di spesa minimo per consegna a domicilio gratuita (Lasciare 0 se non interessati)" %>
  <%= f.input :delivery_time, collection: Store::DELIVERY_TIME_OPTIONS , label: "Tempi di consegna" %>
  <%= f.button :submit, "Conferma", class:"btn btn-light" %>

<% end %>

